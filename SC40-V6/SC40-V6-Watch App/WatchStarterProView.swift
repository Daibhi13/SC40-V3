import SwiftUI

struct WatchStarterProView: View {
    var body: some View {
        Text("Starter Pro View")
            .font(Font.headline)
            .foregroundColor(Color(red: 1.0, green: 0.8, blue: 0.0))
    }
}
